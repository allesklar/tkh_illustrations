<ul class="nav nav-tabs" id="admin-menu-tab">
  <%= content_tag :li, link_to(t('new'), new_album_path), ({ class: 'active' } if controller.action_name.to_s == 'new' ) %>
  <%= content_tag :li, link_to(t('list'), albums_path), ({ class: 'active' } if controller.action_name.to_s == 'index' ) %>
  <%= content_tag :li, link_to(t('edit'), albums_path), ({ class: 'active' }) if controller.action_name.to_s == 'edit' %>
</ul>
